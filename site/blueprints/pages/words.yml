title: Words

options:
  changeTemplate: false

sections:

  terms:
    type: pages
    headline: Terms
    parent: page
    status: all
    templates:
      - term
    create: true
    layout: list
    info: "{{ page.category }} · {{ page.temporal }}"
    sortBy: title asc
